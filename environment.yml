name: durham-solweig  # informational only — env is installed by path (./env), not by name
channels:
  - conda-forge
dependencies:
  - python=3.10
  - gdal
  - pytorch            # CPU build — no NVIDIA GPU on this machine
  - timezonefinder
  - matplotlib
  - sip
  - pdal
  - python-pdal
  - rasterio
  - geopandas
  - numpy
  - cdsapi
  - jupyter
  - pip
  - pip:
      - solweig-gpu
