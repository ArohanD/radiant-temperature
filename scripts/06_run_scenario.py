"""Re-run SOLWEIG with the scenario's Trees + Landcover rasters. Everything else identical to baseline.

The only diff from 04_run_baseline.py is `base_path='inputs/processed/durham_scenario/'`.
Outputs land under `<base_path>/output_folder/`, parallel to baseline outputs.
"""

# TODO: Day 4 — implement after scenario inputs built.
